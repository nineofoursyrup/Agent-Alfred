# Agent-Alfred

本地优先、无框架的单用户私人 AI 助手：同一个 Agent 内核同时服务终端聊天与本地 Dashboard。
状态留在本地文件与 SQLite，模型与模型目录走云端。

本文件只是**词汇表**。设计取舍记在 `docs/adr/`，路线与决策索引记在
[地图 issue #1](https://github.com/nineofoursyrup/Agent-Alfred/issues/1)。

## 对话与循环的三级计数

这三个词各自对应一个独立的计数器，**不得互相替代**。混用会让重试次数与账单口径产生歧义。

**Run（运行）**：
从一条用户请求进入，到最终回复交还用户为止的整个过程。一次 Run 含 1..N 个 Step。
_Avoid_: 会话、对话轮

**Step（步）**：
一次逻辑上的模型响应，以及紧随其后的那一批工具执行。配置项 `max_steps` 限制的就是它。
_Avoid_: iteration、turn、轮、max_iterations

**Attempt（尝试）**：
一次**真实的网络往返**，携带 `attempt_id`。一个 Step 含 1..N 次 Attempt——重试与流式回退各自算一次。
_Avoid_: 请求、retry

**会话（Session）**：
共享同一份**会话记录**的一串 Run。

**会话记录（`agent_log`）**：
跨进程重启存续的对话本身。一次 Run 只在其中留下**一组**用户消息与助手回复，
由 Run 收尾处**独占**写入——任何节点、任何循环都不直接写它。
中间的工具往返属于运行转录，不属于这里。
_Avoid_: 历史

**运行转录（RunTranscript）**：
当前 Run 内供后续 Step 使用的**已提交**消息序列，含中间的工具调用与工具结果。
它随 Run 结束而消失，其中大量消息本就不会交付给用户。
它与会话记录是两个量：把二者合称「历史」，会让「每个 Step 的产出都进了 `agent_log`」
成为一个读得通却完全错误的理解。
_Avoid_: 历史、在途消息

**工作记忆**：
从会话记录取出的最近 N 轮窗口，是**读侧投影**，不是存储，没有写入方。

## 提交与作废

**提交（committed）**：
一次 Attempt 正常收尾，其产出成为该 Step 的**有效结果**并追加到**运行转录**。
一次成功的 Run 里，每个 Step 恰有一次 committed。

**作废（aborted）**：
一次 Attempt 未正常收尾，其产出**不进入历史**，但它消耗的真实 Token 与费用**照常记账**。
流式中断（缺收尾信号）、超时、被重试取代的失败，都产生作废。
作废的产出**不进入运行转录，更不进入会话记录**，只留在追踪与 `AttemptRecord` 里。

作废的产出可以作为**进行中的过程**实时展示，但作废发生时必须显式、可见地撤回——
不得保留、不得与新结果并列、不得当成成功结果入库。

## 消息与内容块

**Message（消息）**：
历史的唯一元素，角色只有 `user` 与 `assistant`，内容是一个**有序的内容块序列**。
一批工具结果就是一条装了多个工具结果块的 `user` 消息——没有独立的「结果批」类型。
_Avoid_: ToolResultBatch

**Block（内容块）**：
Message 内容的最小单位，四类：文本、思考、工具调用、工具结果。保序，可在同一条消息内共存。

**系统提示词**：
人格文件与当前本地时间拼装出的**请求级**输入，每次请求现场生成。
它不是 Message，**永不进入历史**——否则昨天的时间戳会永久污染今天的上下文。
_Avoid_: system 消息、system 角色

## 模型接入

**ModelClient（模型客户端）**：
业务循环唯一认识的模型接口。实际是一条装饰器链，链上每一层都实现同一个接口。
循环感知不到供应商、重试、流式回退的存在。

**Adapter（适配器）**：
ModelClient 链的最内层，只做某一种 wire 形状的编解码，**不含任何时间相关行为**。
`attempt_id` 由它铸造，因此 attempt 数恒等于真实网络往返数。

**ModelEndpoint（模型端点）**：
一个**可连接的目的地**：`endpoint_id` + `base_url` + `auth_ref`。
它与 Adapter 是不同的轴：端点回答「连到哪里、用哪把钥匙」；Adapter 回答「这种 wire 形状怎么编解码」。
同一个端点下的不同模型可以走不同 Adapter，因此**不得**把供应商名当成 wire style。
它**不持有 `wire_style`**——那挂在模型上（见下条）。
**不要叫它 Gateway**：`Gateway` 在本词汇表里是收发信封的**入口适配器**（见「入口与并发」），
承担行为角色；而端点只是一个地址加一把钥匙，二者不可混。
_Avoid_: Gateway、网关、供应商、provider、Adapter

**wire style（线路形状）**：
一个模型实际使用的 API 形状。它挂在**模型**上，不挂在供应商上——
同一个端点的同一个 base URL 下，不同模型可能走不同形状。

**连接四态**：
描述某个外部服务此刻通不通：`未配置 / 已配置未测试 / 已连接 / 错误`。
它**只**描述连通性与认证，不承载任何关于我们自身实现边界的信息。

**支持三态**：
描述我们这一版能不能驱动某个模型：`supported / unsupported / unknown`，默认 `unknown`。
与连接四态**正交**——端点连得上、密钥也对，模型依然可以是 unsupported。
「功能尚未实现」属于这里，把它塞进连接四态即为混用。

## 工具

**工具（Tool）**：
模型可以调用的一个具名能力，由静态声明（名字、描述、入参 Schema、副作用等级）与一个可调用体构成。
它是**代码里的声明**，不持有任何运行时权限状态。
_Avoid_: 函数、function、能力

**副作用等级（effect）**：
一个工具对世界做什么的**静态声明**，三值：`local_read` / `local_write` / `external`。
由工具作者写死，不随配置或用户意愿变化。「需不需要授权」由它推导，不另设开关——
两个字段表达同一件事，就有了不一致的机会。

**可用性（availability）**：
这个能力**配齐了没有**——密钥、依赖是否就位。它的外在表现就是**连接四态**。

**授权（authorization）**：
用户**准不准**这个工具动外部世界，三态：`unset` / `allowed` / `denied`。
它存在用户配置里，**不存在工具上**——否则「改代码来开权限」会变成一条可用的操作路径。

可用性与授权**正交**：只有可用且已授权的 `external` 工具才可执行。
本地工具不进授权维度。合并二者是有诱惑力的，但「配齐能力」与「准许行动」不是同一个事实：
密钥齐备而用户不准，与用户想用却没配密钥，是两种相反的处境。

**引导 Schema**：
`external` 工具在**未配置且未被明确拒绝**时暴露给模型的那份声明：工具名不变、参数置空、
描述如实写明「调用只会返回配置指引，不会执行」。
它存在的理由是让模型能告诉用户「去配这个 key」；
给一份与真实 Schema 一模一样的声明则是在暗示它能跑，属于伪造。

**工具账（tool_ledger）**：
`local_write` 与 `external` 工具每次执行留下的一条记录，用于让**副作用的事实**在对话历史被压缩后仍然存在。
它是**证据，不是锁**：系统不据此拦截重复调用——判不出同参数的第二次调用是不是用户本意，
而「已执行过，拒绝重复」在用户确实想再来一次时就是凭空捏造的策略。
`local_read` 不入账。

`local_write` 与业务写入**同事务**落账；`external` 无法与本地数据库原子提交，
因而走 `started → succeeded / failed / unknown` 的状态机。

**结果未知（unknown）**：
工具账上的一个终态：动作**已经发出**，但结果无从得知。
两个来源——预算耗尽后放弃等待，以及进程被强杀后 `started` 行等不到收尾。
它是一个如实记录的事实，不得读作失败，也不得读作成功。

**预算（budget）**：
一次工具调用被允许占用的时长上限。它**不是超时**：同步函数无法被抢占中断，
因此预算唯一能生效的地方是**启动前**——耗尽即不启动。
启动之后真正的中止只能由工具自己的 IO 层执行。
_Avoid_: 超时、timeout

**两份投影**：
一次工具执行的结果同时以两种形态留存：
**`model_content`**（截断后回喂模型的那份，末尾带显式截断标记）与
**`audit_content`**（完整结果，脱敏后交由追踪落盘）。
两份都要存下来，不能事后按当时的阈值重算——阈值会变，
重算出的是一份**从未存在过的**模型视图。

**工具成本**：
一次工具调用花掉的外部计量（如搜索 credits）。它与模型 token 费用**分列**，绝不相加。
**费用三态**在此照旧适用：拿不到就是未知，不猜、不按零处理。
「不计费」与「应计费但未知」是两回事，必须分得开。

## 用量与费用

**Usage（用量）**：
一次 Attempt 消耗的 Token 明细。每个字段都可以是「未提供」，而**「未提供」不等于 0**。
Token 是费用的唯一事实来源；金额一律在渲染层算出，不随用量存储。

**price_source（价格来源）**：
一笔费用所依据的价格出自哪一档。它必须随费用一同展示——
否则「官方目录拉到的真实单价」与「查表兜底的估算」会被读成同一个可信度。

**费用三态**：
`精确`（端点自报实付，独此一档）/ `估算`（有可接受的价格来源）/ `未知`（拿不到 Token 或无可接受价格）。
`未知` 时**不出任何金额**，只单列调用次数与 Token 量。

**派生导出（derived export）**：
由 SQLite telemetry 显式生成的只读快照（如 `usage.jsonl`），**运行期间不实时维护**。
它携带 schema 版本与生成时刻，含金额时还带 `price_source` 与计算时刻——
否则日后价格链变动引起的重算差异无从解释。删了不损失任何事实，重算即可。
_Avoid_: 用量日志、账单文件

## 追踪与事件

**事件（Event）**：
业务与模型层向外**通知**发生了什么的最小单位，由「信封 + 载荷」两层构成。
信封承载谁 / 何时（`seq` / `ts` / `run_id` / `session_id` / `step_index` / `attempt_id`），载荷承载什么。
事件**只是通知，不是账本**：用量与费用永远直接汇总 `ModelResult.attempts`，
因为 EventSink 可以缺席，而缺席时那笔钱照样花掉了。
_Avoid_: 日志、log

**EventSink（事件汇）**：
事件的消费端接口。**单向**——业务代码无法从它读回任何东西。
发射分**两阶段**：准备在锁外完成，是纯函数，无 IO、无共享副作用、可并发；
提交在短临界区内完成，只做定量的引用入队或状态切换，不按载荷大小复制数据。
四个实现：CLI 渲染、Dashboard SSE、trace 文件、telemetry。
_Avoid_: Observer、观察者

**`seq`（序号）**：
进程内全局单调递增的事件序号，与 `process_instance_id` 合成事件的唯一身份。
它是**发布线性化顺序**——各 Sink 完成准备后进入临界区的次序，
**不是**业务线程开始发事件的时间顺序，因此不可读作因果先后。
**排序只认 `seq`，不认时间戳**——墙钟会被回拨，而把重试排到原始请求之前就是在说谎。

**`trace_policy`（落盘策略）**：
**事件级**属性，`transient` 或 `persist`，表示这个事件该不该写进 trace。
它说的是「应不应该」，**不是**「已经成功写进去了」。
`transient` 只给纯粹为实时渲染而生、审计价值由收尾快照承载的高频事件——
模型的流式增量与工具的进度通知。

**可重放（replayable）**：
事件级谓词，表示 SSE 断线重连时该不该补发。v1 派生自 `trace_policy == "persist"`，
但**与它不是同一个轴**：前者回答「该不该写进 trace」，后者回答「该不该补发给重连的浏览器」。
今天同真是巧合，混用会让两个轴焊死在每一处调用点上。

**`flush_at_run_end`（持久性关键）**：
**Sink 级**属性，表示 Run 收尾时是否必须等待它的 `FlushResult`。
与 `trace_policy` **正交**：SSE 收得到 `persist` 事件却不被等待，
trace 会被等待却照样丢弃 `transient` 事件。

**持久性屏障（flush barrier）**：
Run 收尾处的一个同步点：发出 `run.finished` 后，只等持久性关键 Sink 落定，
再据其结果把回复、Run 级 telemetry 与追踪状态**原子写入同一个事务**。
整套发射机制为了不阻塞业务而异步，这里是唯一一处**故意阻塞**。
刷写结果是一个闭合联合：屏障型只有 `flushed` 与 `failed`，尽力型只有 `best_effort`，
于是非持久性关键的 Sink 在类型层面**说不出**「已落定」这个词。
屏障判定成功的条件是 `flushed` 且未丢弃任何事件；返回缺失、超时、异常与 `failed`
一律置追踪不完整。

**追踪不完整（trace_incomplete）**：
「在该 Run 的持久性屏障处，关键 Sink 写盘未成功」。
它是一个**如实记录的降级**，不是错误——Run 照常完成，回复照常交付。
判读规则：trace 末尾缺 `run.finished` 表示该 Run 未正常收尾；
但若该 Run 已标 `trace_incomplete`，则只能判定**追踪链损坏**，不得据此断言业务异常。

**脱敏（redaction）**：
在 `FanOutSink` 入口一次性完成的敏感值消除，依据是**已加载的密钥值集合**加敏感字段名。
值匹配是其中不可省的一半：从工具结果正文里漏出的密钥不在任何黑名单字段里。
**fail-closed**——脱敏本身失败时替换整个受影响载荷，绝不放行原文。

**重放环（ReplayRing）**：
进程内唯一一份有界缓冲，装的是**可重放事实**，供 SSE 断线续传与当前 Run 重载。
元素是**逻辑事件**而非物理帧，整体入环、整体驱逐——「半个事件」因此无法表达。
它是活跃 Run 的**唯一**重放源：回复与 telemetry 要到持久性屏障才落库，
而 trace 是异步排空的，Run 跑到一半时别处没有可读的完整事实。
_Avoid_: 缓存、事件缓存

**检查点（checkpoint）**：
服务端曾签发过的、位于完整逻辑事件边界上的游标位置。
**不是**落在数值区间内的任意序号——全局序号里夹着暂态与不可重放位置，
算术前驱未必是合法游标。

**逻辑事件与物理帧（event / frame）**：
一个逻辑事件在 SSE 线上可能被切成多个物理帧。切分是**纯传输层**的事，
不外泄进事件协议：一个逻辑事件仍只占一个 `seq`，且只有末帧推进游标。

**传输层通知（transport notice）**：
描述**某一条连接**自身传输状况的通知（重放间隙、增量丢弃），
**不是**事件——不占 `seq`、不带 `id`、不进重放环、不进 trace。
它与领域事件必须分名：把「某个浏览器标签页太慢」写进 trace，
就是让审计件记录与业务无关的事。

## trace 的落盘与保留

**Run bundle（运行包）**：
一个 Run 的全部落盘事实所在的目录：`meta.json` + `trace.jsonl` + `artifacts/`。
它是**完整性、生命周期与导出三者共用的同一条边界**，也是裁剪与租约的最小单位。
_Avoid_: trace 目录、日志目录、run 文件夹

**托管路径（managed path）**：
状态目录下由本系统创建并强制权限的路径（目录 `0700` / 文件 `0600`）。
与之相对的是用户自己维护的路径（Skill 文件），本系统**不递归改写**它们、也不动可执行位。

**`run_storage_id`（存储标识）**：
由完整 `run_id` 确定性摘要得到的 32 位小写十六进制串，**只**指摘要本身。
它存在的理由是 `run_id` 的格式不在任何契约里，而不透明值不进路径。

**`run_dir_name`（Run 目录名）**：
`<HHMMSS>Z-<run_storage_id>`，UTC。它是裁剪器唯一的解析入口，
必须能与 `meta.json` 里的原始 `run_id` 交叉验证。

**发布（publish）**：
Run bundle 从不可见变为可识别的那一刻——在同一日期目录内由 staging 目录
**无覆盖原子改名**为 `run_dir_name`。发布之前的 bundle 不参与任何识别、裁剪或读取。
_Avoid_: 创建、初始化

**截断尾行（truncated tail）**：
`trace.jsonl` 崩溃后允许存在、且**只允许在末尾存在**的一条不完整记录。
读取器据它判该 bundle 追踪不完整；**任何内部坏行一律失败**——
能跳过坏行的读取器会把损坏的审计件读成完好的。

**读取租约（read lease）**：
进程内对一个 Run bundle 的引用占用，由 `TraceStore.acquire_read()` 持有期间
该 bundle 不可被预约删除。持租与预约在**同一把进程锁**下线性化，
否则单有引用计数仍然存在先检查后删除的竞态。

**删除预约（deleting）**：
裁剪器在同一把锁下对一个零引用、非活跃 bundle 做的进程内标记。
读取方见到它必须拒绝增租。它**不落盘**——落盘依据只有恢复状态。

**裁剪（prune）**：
按保留策略删除 Run bundle 的运维动作。它是**独立的生命周期事实**，
与 `trace_incomplete` 正交：后者是 Run 收尾时的观测，前者是几十天后的运维。
_Avoid_: 清理、轮转、过期、归档

**恢复状态（retention state）**：
裁剪的**意图日志**，不是上次报告：删除之前先落盘、失败项留在其中等待下次对账。
它的唯一职责是让 `rmtree` 中途崩溃留下的半删目录能被收尾。

## 记忆

**记忆库（Store）**：
某一类记忆的存放与检索端，可替换。默认实现落在本地 SQLite，
向量或托管实现只保证同一套契约，不改变调用方的写法。
_Avoid_: Adapter、记忆适配器（`Adapter` 专指模型 wire 形状的编解码，见「模型接入」）

**语义记忆**：
与时间无关的事实，形如「主题 + 事实」。回答「关于某人某物我知道什么」。

**情景记忆**：
一段经历的摘要，带发生**区间**（起点必有，终点可无，无则表示瞬时）。
回答「某段时间里发生过什么」。区间而非时刻——一次提炼覆盖的对话可以跨日，
压成一个时刻必然让跨界的经历被时间窗漏掉。

**程序性记忆**：
用户维护的 Skill 文档，描述「该怎么做某件事」。
它来自文件系统、只读、没有写入方，**不在记忆库的可替换框架里**。

**`MemoryId`**：
一条记忆的**不透明**标识，由记忆库铸造。调用方只可原样传递与相等比较，
**不得解析、排序，或据此推断先后**——与「事件排序只认 `seq`」同源：
不给调用方一个可以被误用的结构。

**不透明相关度**：
检索结果自带的一小段仅供展示的文本。它**不是**可比较的分数：
不同记忆库的标度互不相通且方向相反，一旦成为数值，阈值判断换库即静默失效。
检索只承诺**顺序**，不承诺可比的量。

**机械幂等**：
记忆库这一层的去重：规范化后完全相同的内容重复写入不产生第二条，返回原有的 `MemoryId`。
它是数据完整性，任何记忆库都做得到。

**语义归并**：
提炼这一层的去重：同一件事的两种说法，由模型判断是新增还是**取代**已有记忆。
它需要模型，记忆库做不到。与机械幂等**必须分名**——
合用「去重」一个词，会让「两种说法不该变成两条」被误读成记忆库的责任。

**来源（origin）**：
一条记忆是怎么来的，闭合三分支：**提炼**（携批次标识）、
**用户手填**（携录入界面）、**工具写入**（携调用标识）。
它必须随记忆一同展示——否则「模型提炼出来的」与「用户亲口说的」会被读成同一个可信度，
理由与 `price_source` 必须随费用展示完全一致。

**遗忘**：
删除一条记忆。它是**真删**——不留墓碑、不做软删标记，索引同步清除。
且被删的**正文与主题不出现在任何下游**：不回喂模型、不进 trace、不投 Dashboard。
留存的只有标识与不可逆指纹，用来回答「是谁删了哪一条」，
而不足以回答「删掉的那句话是什么」。

**规范化**：
为算出幂等键与指纹而对内容做的**机械**整理：Unicode NFKC、首尾去空白、内部空白折叠。
它**只作用于键，不改写存下来的正文**——否则用户在界面上看到的是被机器改过的句子。
它**版本化**：没有版本号，日后调整规则时旧数据的幂等行为会静默变化。
大小写与标点一律不碰：那已经踏进语义归并的领地，而这一层的契约必须任何记忆库都能复现。

**覆盖（override）**：
用户目录里的同名 Skill **整体取代**内置版本，不合并——Skill 是散文，合并两份散文没有语义。
覆盖**必须可见**：界面上要写明某个内置 Skill 已被用户版本取代。
同一目录内重名则是配置错误，启动即失败，不静默取其一。

## 图

**图（Graph）**：
一张由节点与边构成的**有向无环**执行结构，用来把若干步骤编排成一次可视的流程。
它不改动核心循环——完整的 Agent 循环只是它的一种节点。
_Avoid_: 工作流引擎、DAG 引擎

**Wave（波）**：
图的一层拓扑分层：彼此没有先后依赖、因而可以同时被考虑的一组节点。
它是**分层**，不是线程——同层节点串行执行，但都读取本波开始时的**同一份不可变快照**。
两个名字（wave 与「并行」）之所以要分开，是因为混用会让「同时可考虑」被读成「同时在跑」。

**决议（taken / not_taken）**：
一条边在本次执行中算不算数。它挂在**边**上而不是节点上——
只有边级决议才能同时表达「这一支没被选中」与「这一支的源头失败了」这两件不同的事。

**跳过（skipped）**：
一个节点的终态之一：全部入边都决议为 `not_taken`，因而不执行。
它**不是失败**。失败有自己的终态与自己的出边（error 边），
让失败冒充跳过，等于把一次故障伪装成一次正常的分支选择。

**终点（Terminal）**：
声明为流程出口的节点，两类：**结果终点**（携一个产出）与**无动作终点**（明确判定不需要动作）。
一次执行必须**恰好**成功抵达一个。零个是没有产出，多于一个是两份互相竞争的产出，都是失败。

**无动作（no_action）**：
「这次不需要做任何事」这一结论本身，携一个由终点静态声明的稳定原因码。
它**不产出**任何内容——把它读成「产出了一个空回复」并真的发出去，是伪造。

**拓扑哈希（topology_hash）**：
一张图**静态结构**的指纹，用来判定一条历史轨迹能不能叠加到当前的图上。
它只覆盖结构（节点身份、读写声明、边与标签、终点），不覆盖展示文案与实现代码——
它证明的是结构兼容，不是行为版本。

**恢复（recovery）**：
执行经由 error 边绕过一次节点失败后仍抵达终点。
它必须写进**结果本身**，不能只靠事件流证明——事件汇可以缺席，而缺席时那次降级照样发生了。

**副作用三态**：
本次 Run 到此刻为止动没动过外部世界：`none` / `occurred` / `unknown`。
只有 `none` 允许自动回退重跑；`unknown` 与 `occurred` 同样禁止重跑——
「不知道有没有做过」和「确定做过」在重复副作用这件事上后果一致。

**RunBudget（运行预算）**：
一次 Run 允许消耗的 Step 总量，由图、其中嵌套的循环、以及失败后回退的普通循环**共同持有**。
它是**全 Run 一份**，任何路径都不重新拿满——
让每条路径各持一份，等于把用户配的上限悄悄乘以路径数。

**StepLease（步租约）**：
一次模型响应在发出首个网络往返**之前**取得的一次性凭据，取得即扣减，无论成败都不归还。
后续的重试与流式回退共享同一份租约。
租约不因产出被撤销而回补：撤销能否定产出的有效性，撤不回已经发生的网络往返与账单。

## 入口与并发

**RuntimeHost（运行宿主）**：
一个进程内**唯一**的所有者，持有 `process_instance_id`、`seq` 的分配、共享的 `FanOutSink`、
唯一的数据库写连接、重放环与 `RunCoordinator`。
CLI 前台与 Dashboard 是同一个宿主的两个入口，不是两个进程——
拆成两个进程会分裂重放身份，且暂态事件根本不落盘，Web 侧将永远看不到流式增量。

**入口描述（entry descriptor）**：
宿主取得状态目录进程锁并成功绑定回环地址后原子写下的一份记录，含实例标识、进程号与端口。
它是**唯一**的入口事实：书签、`Origin` 白名单与单实例语义必须指向同一个入口。

**RunCoordinator（运行协调器）**：
Run 的准入端。CLI 与浏览器发起的 Run 走**同一个** `submit()`，
以非阻塞占用保证任意时刻至多一个 Run。
它管的是**准入**，不是执行：接受后交给唯一的执行线程，调用方不得自己跑。

**MutationGate（写入闸门）**：
所有会修改记忆、设置或外部状态的入口都必须经过的同一道闸。
只锁聊天而放行其他写请求，兑现不了全局串行的承诺。

**Gateway（入口适配器）**：
负责收发信封的入口：把一次外部输入翻译成一次 Run，再把结果送回去。
它是一个**行为角色**，不是一个地址——v1 有 CLI 与 Web 两个，
消息渠道（Telegram / 邮件等）是同一角色的未实现实例。
与「模型接入」的 `ModelEndpoint` 无任何关系：那是模型侧的可连接目的地，
本条是外部世界进出本系统的关口。代码落在 `src/agent_alfred/gateway/`。
_Avoid_: 渠道、channel、前端、ModelEndpoint

**entry surface（入口面）**：
一个 Gateway 下可区分的具体接入点，由 `entry_surface_id` 标识。
事件信封里的 `source` 记录消息来自哪个 Gateway；需要更细的粒度时记 `entry_surface_id`。
v1 里两者一一对应，因此这个区分暂时不产生额外结构——
留名是为了将来一个 Gateway 挂多个接入点时不必改信封。
