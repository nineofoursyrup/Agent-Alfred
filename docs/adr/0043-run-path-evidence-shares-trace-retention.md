# 单次执行路径与当时拓扑共享 trace 保留边界

状态：accepted；来源：#73 grill-with-docs 首轮 Q1–Q3，用户“全按建议”确认；完整 GRAPH-RUN-EVIDENCE-DESIGN-r3 随后已获用户“确认”。

新能力启用后的 Run 保存当时绑定的流程拓扑和身份，详细执行路径与历史拓扑随 trace 保留和裁剪，不另建永久逐节点记录。当前发布的图会变化，而裁剪后的摘要不足以证明中间步骤；把新图或最终结果用于补全历史会制造无法核验的事实。

代价是 trace 裁剪后只保留原有 Run、结果摘要及账目，详细路径不可用；旧 Run 缺失的结构和轨迹不回填。相比独立永久路径存储，这一选择复用现有证据生命周期，也接受无法永久查看所有历史细节的限制。

用户随后以第二次“全按建议”确认 Q4–Q8：运行中快照属于进程内观测，沿用异步 trace 与既有收尾持久性屏障，不逐节点同步落盘。常规观测故障使路径降级，不新增业务中止或重试条件；既有 fatal 发布错误语义保持。完整的状态、失败及验收定义集中在 #73 设计/规范中，完整 r3 已获用户整体确认；实施验收权威为 [GRAPH-RUN-EVIDENCE-SPEC-r1 / #81](https://github.com/nineofoursyrup/Agent-Alfred/issues/81)。
