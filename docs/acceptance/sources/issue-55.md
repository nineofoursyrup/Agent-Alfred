# #55 实现：切片④c — 调用工具并看账（Tools 页 + Ops 账本页）

来源：https://github.com/nineofoursyrup/Agent-Alfred/issues/55

## Problem Statement

用户已经能够通过同一个助手聊天、操作内置工具并使用记忆，但还缺少完整的 Tools 与 Ops 用户路径：不知道工具为什么可用或不可用、授权是否真正生效，也难以把一次 Run 的模型尝试、工具请求、执行结果与消耗放在一起核对。

现有工具成本主要随 trace 保存，详细追踪裁剪可能让消耗依据消失；工具请求被拒绝或未启动不一定有完整记录。当前单 Run 费用读取不能直接提供跨 Run 一致汇总，运行中缺少持久账目也不能解释成零费用。这些缺口需要真实持久事实和一致读取边界，不能靠静态页面补成成功。

## Solution

完成“MainBar 调用已交付工具 → Tools 理解能力与授权 → Ops 定位同一 Run、查看请求与成本”的可演示闭环。Tools 管理真实能力与三态授权，执行保持在现有聊天入口；Ops 默认覆盖全局 Run，分别展示模型精确费用、估算费用和未知覆盖，工具成本按服务和单位另列。

工具计量独立于详细追踪持久保存，明确请求、启动确认与业务结果的区别。查询以固定记录及价格快照保持汇总、分页和明细一致；历史参数与两份结果投影只从可验证的原记录按需读取。所有失败、恢复、冲突、裁剪和未确认状态如实呈现。

## User Stories

1. 作为用户，我希望从 MainBar 请求已交付工具并获得真实结果，以便完成任务后核对实际发生的动作。
2. 作为用户，我希望查看宿主真实注册的完整工具清单，以便理解所有已接入能力而不把测试样例当正式能力。
3. 作为用户，我希望在清单看到对模型隐藏的工具及原因，以便理解为什么模型不能调用它。
4. 作为用户，我希望分别查看工具可用性、授权和模型暴露状态，以便区分配置缺失与未获允许。
5. 作为用户，我希望看到未配置外部工具的真实配置引导，以便知道下一步如何配置而不误以为已经执行。
6. 作为用户，我希望看到本地工具不需要外部授权，以便避免把本地能力误读为等待授权。
7. 作为用户，我希望逐工具选择未决定、允许或拒绝并显式保存，以便控制外部行动权限。
8. 作为用户，我希望修改授权时看到正忙原因且请求不被排队，以便避免稍后发生不再符合意愿的变更。
9. 作为用户，我希望旧标签保存冲突时保留草稿并展示当前值，以便核对后再决定新的授权。
10. 作为用户，我希望保存回执丢失时核验当前状态，以便避免自动重送或假称原提交成功。
11. 作为用户，我希望授权文件损坏时看到明确错误并保留原文件，以便修复配置而不丢失原选择。
12. 作为用户，我希望授权故障时继续本地聊天和只读查看，以便不因外部能力故障失去全部助手功能。
13. 作为用户，我希望区分授权已经保存和尚未生效，以便不把文件写入当成执行政策已更新。
14. 作为用户，我希望显式重新应用已保存授权，以便在重建成功后恢复外部工具。
15. 作为用户，我希望同名不同来源工具不继承旧授权，以便避免新能力接管旧权限。
16. 作为用户，我希望查看已移除工具的历史身份与消耗，以便不因卸载而失去历史依据。
17. 作为用户，我希望从工具进入相关 Run 及具体请求，以便追踪该次调用的上下文。
18. 作为用户，我希望在 Ops 默认看到全局 Run，以便同时理解聊天、探针和提炼的消耗。
19. 作为用户，我希望按会话和运行用途筛选账目，以便聚焦某类运行且不切换 MainBar 会话。
20. 作为用户，我希望按工具筛选包含它的完整 Run，以便理解相关运行费用而不伪造工具级模型费用归因。
21. 作为用户，我希望按 Run 查看模型 Attempt 和工具请求明细，以便区分一次运行与其中多次调用。
22. 作为用户，我希望看到真实模型 Attempt 次数并包含重试和作废，以便核对全部真实模型消耗。
23. 作为用户，我希望分别看到工具请求数、已确认启动数和启动未确认数，以便不把请求数量当执行数量。
24. 作为用户，我希望看到拒绝、配置引导、非法输入和批次停止的未执行原因，以便理解为什么有请求却没有动作。
25. 作为用户，我希望同一请求回读旧回执时不重复计算动作和成本，以便获得稳定的历史账目。
26. 作为用户，我希望用新的请求再次执行相同参数的动作，以便不被参数去重错误阻止。
27. 作为用户，我希望trace 裁剪后仍可查看持久工具计量，以便继续核对已记录的消耗。
28. 作为用户，我希望计量登记失败时工具不启动，以便避免产生无法登记的新动作。
29. 作为用户，我希望计量收尾失败后停止后续工具与模型，以便不继续扩大未记录范围。
30. 作为用户，我希望计量存储故障期间看到新 Run 暂停及只读诊断，以便在修复后安全恢复使用。
31. 作为用户，我希望修复后显式发起新请求而不自动重放旧动作，以便避免重复副作用。
32. 作为用户，我希望崩溃后区分明确未启动、已确认启动和启动未确认，以便不把持久意图当作动作证据。
33. 作为用户，我希望同时看到当次调用结果与后续操作核验结果，以便理解恢复后的现状而不抹去原观测。
34. 作为用户，我希望从核验结果跳到相关恢复 Run，以便查看后补证据并避免重复归账。
35. 作为用户，我希望分别看到精确金额、估算金额和费用未知次数，以便不把已知部分误读为完整总费用。
36. 作为用户，我希望展开估算的逐维 Token、单价、来源和降级信息，以便复核金额的计算依据。
37. 作为用户，我希望区分 Token 未报告与真实零用量，以便避免缺失数据被补成零。
38. 作为用户，我希望按服务与单位查看工具成本，以便不把不同 credits 或工具成本与模型金额相加。
39. 作为用户，我希望区分工具不计费、计量未知和历史未记录，以便理解消耗事实的可信边界。
40. 作为用户，我希望运行未结束或记录未落定时看到覆盖提示，以便不把暂时没有账目读成零费用。
41. 作为用户，我希望单条旧账损坏时仍查看其他有效记录及缺口，以便继续核对可读部分而不获得虚假的完整汇总。
42. 作为用户，我希望数据库整体读取失败时看到读取错误，以便不把故障误认成空账。
43. 作为用户，我希望使用同一账目快照查看汇总、翻页及明细，以便让所有数字保持同一口径。
44. 作为用户，我希望价格或后台新账变化时由自己刷新，以便避免阅读过程中金额自动更换。
45. 作为用户，我希望快照过期或服务重启后保留筛选并明确要求刷新，以便不混用新旧账目。
46. 作为用户，我希望看到每次估算的计算时刻，以便理解今日价格重算历史用量可能改变估算。
47. 作为用户，我希望使用今天、近七日、近三十日、自定义或全部历史范围，以便按任务需要核对账目。
48. 作为用户，我希望选择明确的 IANA 时区并在页面看到它，以便让自然日和夏令时边界可解释。
49. 作为用户，我希望看到未开始 Run 使用受理时间的说明，以便理解该次查看的时间归属。
50. 作为用户，我希望先查看安全摘要、结果和成本再展开正文，以便快速判断一次调用发生了什么。
51. 作为用户，我希望查看当次生成保存的模型结果投影，以便核对回喂形态而不把投影存在误认为模型已收到。
52. 作为用户，我希望按需查看完整脱敏审计，以便在需要时了解详细历史证据。
53. 作为用户，我希望以纯文本逐段阅读很长的结果，以便阅读全文而不让浏览器积累大段正文。
54. 作为用户，我希望查看准确配对的模型提交参数，以便理解模型当时提出的要求而不从业务数据反推。
55. 作为用户，我希望参数或正文缺失时看到未记录、裁剪或不可读取原因，以便不被重建的假历史误导。
56. 作为用户，我希望人工历史断线时保留当前段并标为离线，以便继续阅读已有内容而不假称可以续读。
57. 作为用户，我希望重连后重新核验历史并清除已不可读的预览，以便不让旧响应恢复失效正文。
58. 作为用户，我希望保留原始历史的人工查看并维持当前记忆的遗忘限制，以便区分历史证据与自动复用能力。
59. 作为用户，我希望离开详情后释放正文且不建立浏览器持久副本，以便避免额外积累结果正文。
60. 作为用户，我希望看到结果未知或待核验时的准确说明，以便不被统一重试按钮诱发重复动作。

## Implementation Decisions

### D01 交付路径与范围

用户在 MainBar 请求执行已交付的内置工具，在 Tools 查看真实能力、外部授权和调用入口，再在 Ops 定位该 Run、工具请求及实际模型消耗。MainBar 仍是唯一正式对话入口；Tools 不增加手填参数执行台。

Tools 和 Ops 是新增页面，但本片同时包含必要的工具计量、授权持久化与生效、账目快照和历史证据读取建设，不是仅把既有 API 渲染出来。

真实演示使用当前内置工具。可注入 external 工具仅用于验证完整授权矩阵、错误和计量协议；不能把测试工具放入正式清单，也不能称为已验收 Tavily/MCP 真实链路。不接入 Tavily/MCP，不新增 Database、Behaviour、费用换汇、trace 下载导出或付费模型验收。

### D02 Tools 页面

#### 当前能力

目录来自宿主真实注册声明，包含对模型隐藏的已注册工具。不能用 `schemas()` 反推完整目录，因为它只返回模型可见子集，也可能仅有空参数引导 Schema。

每项分别表达名称、说明、来源与能力身份、副作用等级、可用性、授权及模型暴露结果。模型暴露结果解释为真实能力、仅配置引导或隐藏，并给出原因。本地工具标明不需要外部授权；不强套外部连接四态。外部状态只能来自注册集成提供的真实观测，`configured=True` 不证明已经连接；不存在观测时显示尚未验证。

既有六格矩阵保持：未配置且 unset/allowed 为引导；denied 始终隐藏；已配置且 unset 隐藏、allowed 暴露真实能力。授权配置不可读或尚未生效是另外的阻止原因，不能硬塞进该矩阵冒充用户选择或连接失败。

所有历史关联按稳定能力身份处理。当前未注册的历史工具不重新加入可操作目录；Ops 历史行标明当前未注册。来源变更或无法确认身份不能继承旧授权或把旧元数据改写成新描述。

#### 授权写入

用户逐项选择未决定、允许、拒绝后显式保存。授权持久化与模型设置分开拥有，复用其原子写、版本冲突与磁盘指纹检测机制，不把授权伪装成模型指派操作。

新授权存储缺失时以合法初始状态加载；损坏、较新 schema 或不能可靠读取时保留原文件、禁止覆盖并暂停 external 能力。本地聊天和工具不因授权故障被禁用；原有模型配置或计量故障的准入限制仍独立成立。

保存与重新应用均进入现有 mutation gate，忙时 409，不排队。保存验证整个配置版本和磁盘状态，陈旧草稿保留，回读当前值供用户核对；不同标签的选择不自动合并。掉回执仅回读，当前值等于草稿不证明原命令成功。

生效要求模型工具 Schema 与执行检查采用同一政策版本。尽可能在写入前准备可验证的新注册表，持久保存成功后再发布；若已保存后应用失败，则外部能力暂停，明确区分保存版本与生效状态，并提供显式重新应用。重启也必须从有效持久配置完成正常加载和应用后才暴露 external 工具，不能先用旧 allowed 状态运行。

打开页面、恢复焦点、恢复连接、保存回读均核对当前状态。草稿与服务端当前值分别保留；没有当前证据时不显示已经生效。此页不要求新建一套通用 SSE 协议，也不得借用 `memory_revision` 表示授权变化。

### D03 工具计量与身份

#### 对象与持久事实

计量对象是已接受处理的工具请求，唯一身份沿用宿主提供的 `(run_id, step_index, call_id)`，能力身份另存用于归属。`operation_id` 是可选的业务操作关联，不能作为所有请求的必填主键。

模型返回的已提交工具请求批次须在任何一个工具启动前完成必要的持久登记，覆盖配置引导、校验拒绝、未知工具与因前序动作停止而未启动的剩余请求。作废 Attempt 中出现的工具块不是已接受执行请求。重复或无法可靠配对的身份按协议异常处理，不能猜测配对。

新计量记录只保存无正文的来源和身份、可用的时间事实、启动确认、结果/安全原因码、计量状态及单位、关联业务证据和核验来源。不得保存参数正文、模型结果、完整审计或含正文的自由摘要；摘要使用动作、状态等安全元数据。历史旧记录缺少某项元数据时明确未记录，不用当前定义回填当时事实。

工具请求数按唯一请求计，已确认启动数、明确未启动数、启动情况未确认数分别表达。启动确认与结果、成本是不同轴；已启动不代表成功，也不代表收费。

#### 业务账与计量的分工

计量是独立事实来源，不订阅可缺席的事件流来拼账，也不把 `tool_ledger` 当全量调用表。本地只读请求同样有计量覆盖。

既有工具账、外部回执、文件意图和恢复记录继续拥有业务幂等与副作用事实。相同参数的新请求可以执行；同身份旧回执回读不能创造新的动作或成本。计量去重不替代既有业务幂等，不根据参数相同阻止用户的新动作。

SQLite 本地写工具的计量收尾应与可原子确认的业务写及工具账遵守调用方事务所有权；文件和 external 路径不能伪装成 SQLite 原子副作用，须关联既有意图和回执。新的计量协议不能清空仍承担恢复职责的旧业务记录。

#### 失败、恢复与后补证据

登记失败不启动工具。执行后计量结果无法落定时，停止后续工具与模型调用；记录可确认的事实并保留不确定性，不把已执行动作改写为失败或撤销。计量故障阻止新 Run，保留只读诊断；通过持久化及恢复校验后只允许新的显式请求，不自动重放旧动作。

持久意图不证明实际启动。只有能证明进入执行的结果或明确执行证据才确认启动；进程在意图与执行之间中断，恢复后不能硬填“未启动”或“已启动”。控制异常保持其控制语义，不能为了计量而吞掉 KeyboardInterrupt/SystemExit。

调用当时的结果与原操作当前核验结果分别读取和展示，后者带核验时间、证据来源及关联 Run。原操作的恢复不重复增加请求或成本；恢复 Run 自己的新 Attempt 仍正常计量。业务成功不能直接推出成本已知，后补计量必须有计量证据。

### D04 成本与覆盖范围

模型计量沿用现有 Attempt 事实与价格链，使用 `(endpoint_id, model_id)` 和逐维价格来源。重试和作废同样计量。端点有效自报费用为 exact；估算输出逐维 tokens、单价、来源、金额与降级元数据；unknown 不输出该笔金额。

汇总分列精确金额、估算金额和已知存在但费用未知的 Attempt 数，不把已知部分写成完整总费用。每个 Token 维度保留缺失与零的区别；汇总某维度缺失时须标明缺失覆盖，不能将未报告量补零或互推两套缓存口径。

工具成本按稳定服务归属和单位分组，与模型金额分列。区分明确不计费、可解释的已报告计量、应计量但未知，以及旧记录根本未记录。无效数字或缺少可信归属不能作为金额/单位数参与求和；不得使用浮点累计、跨服务 credits 相加或擅自换汇。

实现核查发现：现行 `ToolCost` 仅为 `units: float, unit: str`，原工具规范没有枚举 kind/source 的准确成员，后续 Tavily 票要求 Decimal 与闭合计量协议。本片须补齐可验证的 Decimal 数值、状态和注册服务声明的单位/来源边界；不凭空声称这些字段已经实现，也不为未来服务设计未使用的价格计算器。具体服务报告到通用计量的映射仍归对应接入票；测试夹具必须通过同一声明和验证入口。

缺少完整 telemetry 的 Run 单独标记覆盖缺口，不能由 `attempts=[]` 推断零调用。知道 Attempt 存在但费用未知，与无法确定一个 Run 有多少 Attempt 分别计数。单条坏记录隔离后可读部分仍可展示，页面明确覆盖不足；数据库整体读取失败时不返回空账或零费用。

没有 Run 身份的旧消息不伪造为 Run，也不纳入本片 Run 汇总；明确历史覆盖边界，不删除原消息或旧 telemetry。有端点自报金额但历史模型身份丢失时可保留该已知金额；其余不能用当前指派猜历史模型。

### D05 Ops 查询与账目快照

默认全局 Run，支持时间、会话、运行用途、工具身份及精确 Run 定位。按工具筛选意味着包含该工具的完整 Run，模型费用与其他工具成本仍属整条 Run；高亮匹配请求，不作工具级模型成本归因。

时间初始范围为浏览器 IANA 时区的最近 7 个自然日，另有今天、近 30 日、自定义与全部历史。服务端验证时区和半开区间，按当地日历换算 UTC 边界，不能固定减去 24 小时倍数。Run 归属用开始时间，未开始时用受理时间并注明；某次查看的归属随快照固定，之后开始或核验变化在刷新后反映。

终态列表沿用持久活动序号的稳定排序原则，时间用于筛选而非替代 `activity_revision`。快照由同一数据库一致读取得成员、计量事实及记录状态，同时冻结实际使用的价格集合；不能仅存计算时间而让各次 quote 读取不同的活目录。

汇总、分页和展开财务明细必须绑定同一快照、规范化筛选和进程实例。跨页不重新定价，不将新 Run 加入旧汇总。Ops 跳转到运行详情时仍携带该快照的计量上下文，避免既有独立 Run 读口暗中按新价格计算。

进行中、正在保存和记录失败的 Run 有清楚状态。仅已经持久确认的事实参与汇总，正在运行不显示实时估算；缺口提示说明当前覆盖。新账产生只提示可刷新，不悄悄替换眼前金额。

账目快照只保存无正文财务数据。操作当前核验结果是独立当前读数，标出核验时刻，不能改写快照中的金额或当时观测。正文、工件存在性和当前授权也不由财务快照冻结。

工程起始默认值：快照有效期 15 分钟，单次列表 50 行，全宿主最多 8 份快照和 64 MiB 快照数据；它们是可调整的资源默认值，不是新产品能力。达到资源上限时明确拒绝创建或使旧快照失效，不静默截断“全部历史”并冒充完整结果。页面保留筛选并说明如何重新查询。API 名称、缓存结构和这些限额可在施工中按验证调整，但不得改变同次查看一致、失效不自动换账的合同。

快照过期、被回收或服务重启后，已加载财务内容标为旧快照；后续翻页或展开未加载财务明细要求显式刷新。刷新失败不把旧数据改成新数据，也不丢失筛选。

### D06 调用详情与历史审计

先呈现安全摘要、请求/启动确认、当次结果、成本及当前核验入口；展开默认展示当次生成并保存的模型结果投影，另有完整审计入口。投影存在不证明已发送或被模型收到；明确因删除、待核验、预算或计量失败停止后续模型时，标明未发送，其他情形无证不得声称送达。参数只从准确匹配的 committed Attempt 工具调用块读取并标为模型提交参数，不能从 aborted Attempt 或其他 Run 补齐。

完整审计和模型投影都以当时实际保存的脱敏历史为依据，不能按今日截断阈值重算旧模型视图。既有敏感字段与密钥值脱敏规则保持，未知结构脱敏失败不放行原文。事件正文与工件只用于人工查看，不建可搜索或自动复用的副本。

原始会话和删除前既有 trace 的人工查看按 ADR-0007 保留；当前受管记忆视图仍走原有遗忘边界，二者不能混用。新的删除回执依旧不含被删正文。

长正文采用纯文本分段，工程起始值为每段最多 256 KiB 的编码字节，边界正确处理 UTF-8；只保存当前段，显示总大小和范围，不自动预读全文。落盘的 256 KiB 内联/工件分界不是页面可阅读全文的上限。原已保存内容可继续分页阅读；工件缺失、变更、裁剪、读取故障分别解释。不增加任意路径或原始文件下载。

历史读取只接受宿主确认的 Run、调用与段游标，服务器解析受管 bundle 和工件关联，校验目录/文件身份、普通文件类型、事件身份与顺序、大小和实际内容边界。复用安全文件能力并补齐与裁剪协调的读取所有权，不能声称现有路径式读口已经具备完整读租约。读取、断连、异常、HTTP 关闭均须释放句柄，并在资源释放前排空在途读取。

人工历史断线时保留当前段并标离线，停止继续加载。重连重验后才能续读；当前不可读取则清预览并解释。离开详情清正文，不写 localStorage/IndexedDB/Service Worker 正文缓存；HTTP 沿用 no-store 和纯文本安全呈现。迟到响应必须校验当前详情身份、连接代次与读取请求，不能恢复已离开的正文。

### D07 实现责任与可复用边界

| 责任 | 可复用底座 | 本片必须补齐 |
| --- | --- | --- |
| 完整工具目录与授权 | ToolRegistry 静态声明、现有 policies、模型设置原子写机制 | 稳定能力身份、完整目录投影、独立授权存储、保存/生效状态与一致政策发布 |
| 工具请求计量 | Run/Step/call_id、业务账及恢复回执 | 无正文持久计量、批次覆盖、失败隔离、启动确认、证据修订与再准入 |
| 账目读取 | runs.telemetry、project_cost、稳定 Run 分页 | 历史兼容读取、覆盖缺口、混合汇总、冻结价格和记录事实的快照 |
| 人工历史读取 | 中央脱敏、Run bundle、受管文件能力 | 参数配对、两份投影和工件读取、分段游标、裁剪协调与资源生命周期 |
| 页面与 HTTP | 既有 MainBar、运行详情、mutation gate、DOM textContent | Tools/Ops 路由、表单反馈、跨标签重读、快照上下文与失效处理 |

宿主业务层拥有上述读写与生效接口；HTTP 只做参数与来源校验、调用和响应映射，不能自己重建工具或直写 SQLite。模型工具执行、文件恢复和浏览器都消费同一计量事实，测试必须从公共路径验证。

涉及表结构时使用新的前向迁移，不改已发布 DDL。新增状态目录文件须登记现有状态管理所有权；只迁移已知历史形状，不用 UI 工程为旧未知格式编造账目。模型依赖继续可注入，验收不要求外部密钥。

## Testing Decisions

### 主要测试边界

沿用用户已整体确认的最高公共边界：真实 RuntimeHost、临时 SQLite/FTS、真实 ToolRegistry、记忆服务和 HTTP/SSE，由浏览器完成 MainBar → Tools → Ops 的用户路径。模型外部依赖通过既有 ModelClientFactory/ScriptedModel 注入；真实 Attempt 协议与成本证据需要时沿用 Adapter 加本地假传输的先例，不另建假的宿主、业务账或 API 成功响应。

高质量测试断言外部可观察行为：实际动作、持久事实、请求回执、HTTP 状态、页面状态和跨重启结果。不得只断言私有函数调用次数、手工拼出的 UI 模型或复制实现算法。时钟、模型 IO 和真实服务边界可注入，业务链路保持真实。

少量低层测试只补最高入口难以精确制造的边界：新旧 schema 升级、原子持久化失败、控制异常、文件身份/裁剪竞争和价格数值验证。新接缝优先放在宿主拥有的业务服务边界，不让页面绕开既有 RuntimeHost 或拆出多套计量实现。

### 现有先例与覆盖模块

- 沿用会话/工作记忆的 RuntimeHost 提交与等待、真实临时数据库及 ScriptedModel 替换先例，覆盖 Run 与计量生命周期。
- 沿用工具注册表的公共执行、外部意图/回执、文件恢复与无 EventSink 用例，覆盖请求身份、未启动和成本不重计。
- 沿用运行详情的 Attempt 账目和 trace 不可用降级用例，覆盖模型价格投影与历史缺口。
- 沿用 Models 设置的双冲突判据及持久化发布顺序，验证授权保存与生效；不能把其不完整的前端反馈当作完备样板。
- 沿用 Memory 和运行页面的真实浏览器双标签、断线、迟到回读及 HTTP 关闭排空先例，验证 Tools/Ops 的对应行为，不外推 Memory 正文规则覆盖全部人工历史。

### 必须通过的 36 条验收

| ID | 场景与必须观察到的结果 | 决策 |
| --- | --- | --- |
| A01 | 临时 Host、SQLite 与 ScriptedModel 经 MainBar 调用真实内置写工具；Tools 定位能力，Ops 定位同一 Run/请求，业务结果、Attempt 与计量相符，无直接执行表单 | Q1–Q3、Q12 |
| A02 | 真实目录仅含实际注册工具；被模型隐藏的注册工具仍可管理，本地工具不显示 external 授权开关；正式目录无测试外部能力 | Q1、Q7 |
| A03 | 注入 external 的 configured×unset/allowed/denied 六格，真实 Schema、空参数引导、隐藏及执行检查逐格一致；未验证不显示已连接 | Q7、既有矩阵 |
| A04 | 授权保存经过 mutation gate；运行、记录待落定时立即 409，无排队或中途策略切换；成功后 Schema 和执行检查均见新版本 | Q7、Q25 |
| A05 | 两标签旧版本或磁盘外部变更保存失败；原字节与生效状态不被覆盖，草稿保留并能比较当前值 | Q20 |
| A06 | 保存响应丢失、损坏或迟到时先回读且不自动重送；值相同不伪造原提交回执，旧响应不能覆盖较新状态 | Q20 |
| A07 | 缺授权文件得到合法初始状态；坏文件/新 schema 保留字节，external 暂停而本地聊天可用，错误不冒充 unset/denied/连接失败 | Q19 |
| A08 | 写入成功后注入应用失败，显示已保存尚未生效且不沿用旧 allowed；显式重新应用或正常重启完成一致应用后才恢复 external | Q25 |
| A09 | 同名不同来源能力不继承授权；历史仍指向原身份，当前未注册时无授权操作；同一已确认身份不因改显示名变成另一能力 | Q17、Q21 |
| A10 | local_read、local_write、external 夹具均有工具请求计量；无 EventSink 也记录；拒绝、引导、非法参数、未知工具和批次停止剩余请求均明确未启动 | Q9 |
| A11 | committed 请求按 Run/Step/call_id 唯一配对；aborted 工具块不计成已接受请求；同身份回执回读不重复计动作/成本，新身份同参数仍可构成新动作 | Q9、Q12 |
| A12 | 工具计量登记持久化失败时工具体未进入；执行后计量收尾失败停止后续工具和模型，动作事实不被改写成失败或自动撤销 | Q13 |
| A13 | 请求登记后、执行前、执行中和返回后收尾前分别中断；恢复保留能证明的事实，启动未确认单列，不以持久意图冒充启动，不把控制异常变普通成功 | Q14 |
| A14 | 本地 SQL 写与业务账/计量收尾遵循调用方事务；回滚不留下成功证据；文件/external 按真实恢复协议显示不确定，既有回执保留 | Q4、Q13–Q15 |
| A15 | 计量故障后新 Run 被拒，历史只读仍可用；修复和持久化/恢复验证后可发新请求，旧动作不自动重放；单笔业务 unknown 不永久锁死健康存储 | Q26 |
| A16 | 后一 Run 核验原文件操作成功，原调用观测保留并出现独立当前核验；链接正确且原成本不重计，恢复 Run 的新 Attempt 正常计量 | Q15 |
| A17 | 模型 exact、estimated、unknown 混合时分别汇总；不显示不完整总费用，aborted/retry 仍计；逐维来源、缓存口径、缺失与零保持区别 | Q5、Q12 |
| A18 | 工具不计费、已报告计量、未知、旧未记录分别显示；不同服务同名 credits 不相加，不与 USD 模型账合并；非法计量数据不能参与求和 | Q4、Q5 |
| A19 | trace 裁剪/损坏后仍读持久模型与工具计量；正文按可用性降级，Run 业务结局、trace_incomplete 与裁剪事实各自保持 | Q4、Q6 |
| A20 | 进行中、保存中、记录失败、终态缺 telemetry 各有明确提示；空 attempts 不自动当零，已独立持久工具事实可见并注明缺口 | Q10、Q16 |
| A21 | 单条旧记录损坏不抹掉其他有效行，未知 Attempt 数不猜；数据库整体读取失败不变空账；无 Run 旧消息不制造 Run | Q16 |
| A22 | 模型身份缺失但有有效端点报告金额仍保留精确事实；其余不能用当前指派猜历史价格；完整零调用与未知覆盖可区分 | Q5、Q16 |
| A23 | 列表、汇总、翻页、展开和跳转 Run 的金额使用同一记录/价格快照；期间新增 Run、修改覆盖价或目录换代不混入旧结果 | Q11、Q12 |
| A24 | 刷新后统一重读重算并标计算时刻；旧快照期满/被回收/重启后停止加载新财务明细，保留筛选及旧状态，刷新失败不混批 | Q11、Q18 |
| A25 | 资源上限下拒绝或明确失效，不将截断成员集当全部历史；快照无参数和结果正文，查询后无长期占用的 DB 事务 | Q11、Q18 |
| A26 | 7 自然日、今天、30 日、自定义半开区间在夏令时前后正确；非法时区拒绝不静默 UTC；开始时间/未开始受理时间归属按快照固定 | Q8、Q22 |
| A27 | 全局包含 chat/probe/consolidation 等实际 Run，筛选会话不切 MainBar；工具筛选保留匹配 Run 全部账目并高亮相关请求，不冒称模型费用归因 | Q3、Q24 |
| A28 | 模型投影保持当次脱敏和截断事实，审计按需读取；当前阈值变化不重算旧投影，缺失时不反推正文；删除、待核验、预算或计量停止使投影未进入下一模型请求时，不能仅凭投影存在显示已发送/已收到 | Q6、Q13 |
| A29 | 提交参数仅来自唯一配对的 committed 历史块，敏感字段及已知密钥被遮罩；aborted、跨 Run、重复身份和缺失参数不误配 | Q28 |
| A30 | 超长内联和工件审计可纯文本逐段读完；UTF-8 边界不丢重字符，当前段范围准确，浏览器不累计全文，不执行 HTML | Q27 |
| A31 | 任意路径、伪造工件引用、符号链接、跨 Run 目录、文件替换及内部损坏均被拒绝；读取与裁剪竞争有确定结果，不读取被替换文件 | Q6、Q27 |
| A32 | 页面切换、断开、异常、关闭 HTTP 时，正文请求和文件句柄正确释放；在途读取排空后才释放宿主资源，迟到读取不恢复离页正文 | Q23、Q27 |
| A33 | 历史审计断线保留当前段并标离线、不可续读；重连重验发现裁剪/不可读则清正文；HTTP 与浏览器不持久缓存正文 | Q23 |
| A34 | 删除记忆后当前受管视图与自动复用遵守遗忘限制，新的删除回执无正文；删除前原 trace 仍可人工查看，不产生新的可复用正文库 | 既有 ADR-0007、Q6 |
| A35 | 错误说明区分明确未执行、确定失败、启动/结果未知和待核验；execution_error 不统一导向改参数重试，无隐式重试/取消/直接执行入口 | Q2、Q13–Q15 |
| A36 | 原有会话、工作记忆、遗忘/提炼、模型费用、工具幂等、浏览器重连与 HTTP 关闭回归通过；依赖可注入、无 key 的默认全离线门禁通过 | 既有仓库门禁 |

施工须通过项目规定的 Ruff、默认全离线确定性测试、浏览器测试、类型检查、skills 与环境示例校验、wheel/sdist 干净安装验证。external 夹具覆盖六格授权矩阵和计量形态，但不代表真实 Tavily/MCP 或付费服务验收。最终记录实际命令、结果与候选身份；本次规范发布没有运行这些功能测试。

## Out of Scope

- Tavily、MCP 的真实接入及其服务专属计量映射，不为其创建正式假能力。
- Tools 手填参数执行台、自动重试副作用、运行中伪取消和授权写入排队。
- 在途实时费用估算、跨服务 credits 合计、工具成本与模型金额相加、费用换汇及新的工具价格计算器。
- Database、Behaviour、Graph/workflow、完整 Run bundle 下载导出、单独原始工件下载。
- 可搜索或自动复用的结果正文库、浏览器持久正文副本、由当前数据反推缺失历史。
- 无 Run 旧消息的合成 Run、未知旧 schema 的猜测迁移，以及追溯擦除原始会话或删除前既有 trace。
- 本次发布不执行实现、付费模型调用、Git 提交/推送/合并或部署。

## Further Notes

- 本规范综合用户分六轮确认的 Q1–Q28及随后的整体确认，原决策为[决定：切片④c — 调用工具并看账（Tools 页 + Ops 账本页）](https://github.com/nineofoursyrup/Agent-Alfred/issues/32)，归属[地图：Agent-Alfred v1 — 本地优先、无框架、可追踪的私人 AI 助手](https://github.com/nineofoursyrup/Agent-Alfred/issues/1)。
- 当前核实代码基线为 `c002a963676d5d26526c24be882c1dfc2a0af4ce`；本规范中描述的新增页面、授权存储、计量与快照能力尚未实现。开工前再次核对远端基线、工作区归属和最新原生依赖。
- 实现前置能力来自已交付的[ToolRegistry 与内置工具](https://github.com/nineofoursyrup/Agent-Alfred/issues/19)、[看见记忆（Memory 编辑页）](https://github.com/nineofoursyrup/Agent-Alfred/issues/46)和[模型目录、价格链与费用账本语义](https://github.com/nineofoursyrup/Agent-Alfred/issues/15)。Tavily/MCP 不构成本票开工依赖。
- 本规范自身包含用户路径、实施约束和全部验收，执行者无需访问本次聊天或未提交的本地设计文档。实现时同步领域词汇和必要 ADR；沿用中央脱敏、调用方事务、全局 mutation gate、Run 记录、真实遗忘及追踪生命周期的既有裁决。
- 准确用词：工具请求不等于实际启动；持久意图不证明动作发出；保存的模型结果投影不证明模型已收到；授权保存不等于生效；业务 unknown 不等于失败或计量存储故障；未报告不等于零。
- API 名称、存储布局、迁移序号和明确可调的资源默认值由执行者基于当前代码确定，不得借此改变用户语义。计量协议中的服务单位与来源由注册声明验证，不凭未定义枚举臆造既有合同。
- `ready-for-agent` 表示规范已经具备可执行范围与验收，不表示功能已完成或真实外部链路已验收。


## Comment https://github.com/nineofoursyrup/Agent-Alfred/issues/55#issuecomment-5647275798

<!-- issue-55-release-closeout-final -->
Issue #55 已按已确认规格完成实现、独立复评、PR 合并与两阶段 CI 验证。

- 实现 PR：#56，提交 `4dd9ab12f89386e02c6f471e4fa8b97ab6eb2392`，合并 `f7ea73455a966a5e6fdb6f3e1630e0442a4fba90`。
- 门禁修复 PR：#57，提交 `ebd8c1db8476e239b6d33a4c124717488a81c02a`，最终合并 `796b88c7d1e134906e25cf42f259d20381017fc2`。
- 独立 Standards：PASS，0 项剩余发现；独立 Spec：PASS，0 项剩余发现。原三项 P2 已修复并通过成功断言复核：计量故障不抢占在途 Run 的 409 语义；快照失效有独立提示和保留筛选的显式刷新路径；坏授权文件即时显示待核验并遵循活动 Run 政策边界。
- 本地最终门禁：离线 3834 passed、1 requires_key deselected；浏览器 157 passed；Tools/Ops 定向 62 passed（复用生产代码未变的 v5 证据）；Ruff、TypeScript、skills/env、构建、wheel/sdist 新虚拟环境隔离安装通过。
- 原实现 PR CI：[34703124735](https://github.com/nineofoursyrup/Agent-Alfred/actions/runs/34703124735)，绑定实现提交，success。
- 修复 PR CI：[34705150300](https://github.com/nineofoursyrup/Agent-Alfred/actions/runs/34705150300)，绑定修复提交，success。
- 最终合并后 CI：[34705696722](https://github.com/nineofoursyrup/Agent-Alfred/actions/runs/34705696722)，绑定合并提交，success。
- 原实现 v5 的 55 个候选文件、权限和完整 diff 与被测/被审版本一致；完整 diff SHA-256 `4163391dbf58ef3a03ca9d94f31181c558f875c343a497f9ad9658a7af36eacc`。

首次合并后 CI 34703627941 在原有浏览器批次轮询中失败。PR #57 只修改两个测试文件：对明确的 `409 memory_changed` 有界重读，其他异常保留 HTTP/正文诊断并立即失败。真实写入触发 revision 冲突的确定性回归覆盖恢复及成功回执，六类错误响应检查全部通过；历史 CI 未保留响应正文，不推断其确切响应。修复候选同样经过独立 Standards/Spec PASS（0 项发现，各自 10 项回归通过），完整差异 SHA-256 `66f9314d9a2b3e850e769b26e881e76ef6f51dde128d988b467cebc36947cec9`。

真实 Tools registry、授权保存/应用、独立持久工具计量、完整 Run 账目汇总及不可变快照、模型费用/工具服务单位分组、经校验的分段人工历史均已交付。[实现与验收映射](https://github.com/nineofoursyrup/Agent-Alfred/blob/4dd9ab12f89386e02c6f471e4fa8b97ab6eb2392/docs/implementation/issue-55-tools-ops.md)。

A01–A36 逐项结果：

| 验收 | 实现范围 | 结果 |
| --- | --- | --- |
| A01 | MainBar / Registry / Tools / Ops | PASS |
| A02 | Host 实际目录与隐藏工具 | PASS |
| A03 | Schema 与执行授权同源 | PASS |
| A04 | Host mutation gate | PASS |
| A05 | 整配置版本、磁盘指纹、草稿 | PASS |
| A06 | 回执与回读分离、连接代次 | PASS |
| A07 | 不可读授权与本地可用性 | PASS |
| A08 | 保存后应用失败、显式恢复 | PASS |
| A09 | 来源加能力稳定身份 | PASS |
| A10 | 批次登记、各 effect / 拒绝 / 未执行 | PASS |
| A11 | 唯一已提交调用身份 | PASS |
| A12 | 计量故障阻断后续执行 | PASS |
| A13 | 中断窗口与启动证据 | PASS |
| A14 | 调用方事务、业务恢复 | PASS |
| A15 | 计量健康恢复与只读 | PASS |
| A16 | 原观测和当前核验分离 | PASS |
| A17 | 模型逐 Attempt / 逐维费用 | PASS |
| A18 | 工具计量闭合与服务/单位分组 | PASS |
| A19 | trace 与账务独立 | PASS |
| A20 | 进行中、保存与未知覆盖 | PASS |
| A21 | 单行隔离、整体读取失败 | PASS |
| A22 | 精确报告与身份缺失 | PASS |
| A23 | 同一不可变账目快照 | PASS |
| A24 | 到期、刷新、重启边界 | PASS |
| A25 | 有界快照、无正文、释放事务 | PASS |
| A26 | 自然日与 IANA 时区 | PASS |
| A27 | 完整 Run、全局与用途筛选 | PASS |
| A28 | 当次保存投影与实际发送区分 | PASS |
| A29 | 唯一 committed 参数与脱敏 | PASS |
| A30 | 纯文本、UTF-8 分段 | PASS |
| A31 | 受管文件身份与 prune 协调 | PASS |
| A32 | 请求排空、失败清理、离页 | PASS |
| A33 | 离线副本、重连核验、无缓存 | PASS |
| A34 | 人工历史与遗忘自动复用边界 | PASS |
| A35 | 结果未知与未执行说明 | PASS |
| A36 | 既有功能与安装回归 | PASS |

边界：真实 host 交付内置本地工具；external 授权/计量以 fixture 验证，未宣称 Tavily/MCP 真实接入。未运行真实付费模型、未迁移真实用户数据库。本票外的服务接入与相邻票保持各自范围。

本票按 completed 原因关闭。
